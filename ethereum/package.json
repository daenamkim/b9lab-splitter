{
  "name": "b9lab-splitter",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:daenamkim/b9lab-splitter.git",
  "author": "Daenam Kim",
  "license": "MIT",
  "scripts": {
    "compile": "yarn truffle compile",
    "deploy": "./run-ganache.sh && yarn truffle deploy && ./stop-ganache.sh",
    "migrate": "./run-ganache.sh && yarn truffle migrate && ./stop-ganache.sh",
    "test": "./run-ganache.sh && yarn truffle test && ./stop-ganache.sh"
  },
  "dependencies": {},
  "devDependencies": {
    "ganache-cli": "^6.4.4",
    "truffle": "^5.0.26"
  }
}
